# Decision Log — Powerhouse

| Date | Decision | Rationale |
|---|---|---|
| May 2026 | Laravel + Vue SPA | Consistent with MyOrderPad; better suited to application logic than WordPress |
| May 2026 | Laravel Passport for OAuth 2.0 | Central auth server for all Whitedash products |
| May 2026 | MySQL | Consistent with existing stack |
| May 2026 | HTTPS for GitHub remote | No SSH keys configured on dev machine |
| May 2026 | Products table database-driven | Supports adding future products without code deploys |
| May 2026 | Powerhouse never commercialised | It is Apostolos's operating layer, not a product |
| May 2026 | Each product control panel investor-ready | Any product can be spun out independently of Powerhouse |
| May 2026 | Universal customer account via OAuth 2.0 | One Whitedash relationship per customer; brand identity |
| May 2026 | Multi-entity invoicing from day one | Future LTD companies must be supportable without schema changes |
| May 2026 | Commission rules use JSON config | Flexible for all models; new products need no schema changes |
| May 2026 | Security sprint before any customer data | FormRequests, Policies, headers, encrypted casts, rate-limit, audit log; SECURITY.md captures the deploy checklist |
| May 2026 | Session-based staff auth, not Passport | Passport is reserved for OAuth API (product control panels). Staff use Laravel's `web` guard. |
| May 2026 | Login throttle = 5 attempts per 15 min per (email+IP) | Spec is "5 per minute"; widened the window to 15 min — same allowance, blocks slow distributed brute-force, clears on successful login |
| May 2026 | `Model::preventLazyLoading` enabled in non-production | Forces explicit eager loads. Catches N+1 in development and CI without slowing production |
| May 2026 | Sensitive billing fields encrypted at rest | `sort_code`, `account_number`, `account_name`, `qbo_access_token`, `qbo_refresh_token`, `two_factor_secret` cast as `encrypted`. Columns widened to TEXT. |
| May 2026 | `RedactSensitiveData` Monolog processor on stack | Anything matching password/token/secret/sort_code/etc keys is logged as `[REDACTED]` |
| May 2026 | PKCE (S256) enforced on every `/oauth/authorize` request | Prevents authorisation-code interception. Spec called for a `code_challenge_method` column update; Passport 13's `oauth_clients` table has no such column, so enforcement moved to a `RequirePkce` middleware that gates the authorize endpoint instead. Works for both confidential and public clients. |
| May 2026 | `hash_equals()` for every token / signature compare | Prevents timing attacks on cryptographic equality. Banned `==`/`===` for these values; documented in CLAUDE.md. Grep audit at sprint start found zero existing violations. |
| May 2026 | `FileUploadService` centralises all uploads | UUID filenames, real-byte MIME check, EXIF stripped via Intervention re-encode to JPEG 85, SVG sanitised, stored on a dedicated `private` disk. Defends against MIME spoofing, path traversal, and embedded XSS payloads. |
| May 2026 | `webhook_events` table for idempotency | Unique on `(source, event_id)`. Every webhook handler must consult `WebhookIdempotencyService` before processing. |
| May 2026 | Rate limiting on listing endpoints (`throttle:60,1`) | `/customers`, `/invoices`, `/referrers` capped at 60/min/user. Slows credential-stuffing-style scraping. Mass-export detection (`DetectMassExport` listener, 50/10min threshold) fires `security.mass_export_detected` + emails super admin. |
| May 2026 | `App\Rules\NotInternalUrl` validation rule | Prevents SSRF. Rejects any URL whose host resolves to loopback, RFC1918 private space, link-local (incl. AWS metadata 169.254.169.254), CGNAT, or IPv6 ULA / link-local. Mandatory on every user-supplied URL. |
| May 2026 | Invoice edit allowed for draft / sent / overdue | Paid and void invoices remain immutable. Sent invoices may need correction (wrong VAT, typo in line item, address mistake) before payment is received — locking them at draft-only forced staff to void + recreate, losing the audit chain. Policy widened in `InvoicePolicy::update()`. |
| Jun 2026 | Public submit-ticket (hub Phase 3) | `/support` lets a guest open a ticket without an account. `support_tickets.customer_id` made **nullable** (+ `guest_name`/`guest_email`/`guest_phone`) — guest tickets carry submitter details instead of a customer FK. A shared `App\Services\TicketIntakeService` is the single intake path (used by both the public controller and a new `create_ticket` WorkflowEngine action): it creates the ticket + first `SupportMessage` (sender_type=customer, source=web) + an admin triage Task (assigned to the resolved triage user — `tasks.assigned_to` is NOT NULL, so a truly unassigned task isn't possible without a schema change; it lands in the triage queue), all in one transaction, then sends the `SupportTicketReceived` acknowledgement **synchronously** after commit (no queue-worker dependency). The triage Task's `created_by` resolves to `config('support.triage_user_id')` if that user exists else the first super_admin — never a magic id — so the in-transaction Task insert can't FK-fail and drop a guest's ticket. The Task soft-links to the ticket by reference (no `tasks.ticket_id`). Anti-abuse: honeypot + route throttle + **Cloudflare Turnstile** (server-side siteverify via the Http client; `services.turnstile.bypass` skips it in tests/keyless-local only, never prod). Support placeholders flipped live (header nav, landing door, KB "still need help?"); `/apply` stays "Soon". No product picker on the public form in v1 (service still accepts an optional product_id). | 
| Jun 2026 | Public knowledge base (hub Phase 2) | `Public\KnowledgeBaseController` serves `/kb` (index, paginate 20, title+content search, category filter) and `/kb/{slug}` (throttle:60,1; simple every-hit `views++`). Every query is scoped to `is_public && is_published` — the public IDOR-equivalent, so internal/unpublished articles 404 on direct slug access. Article bodies render via a new `App\Services\KbContentService`: `render()` detects HTML-vs-Markdown (same logic as staff HelpController), `renderSafe()` additionally runs the output through **`symfony/html-sanitizer`** with a strict allow-list (safe elements only; link schemes limited to http/https/mailto; anchors forced to `rel=nofollow noopener noreferrer target=_blank`) before it reaches `v-html` on the public page. The Phase-1 "Knowledge base" placeholders (header nav + landing door) went live and the featured-help strip (top 6 by views, self-hiding) was restored on the landing. `/support` (Phase 3) and `/apply` (referral Phase 2) stay "Soon". Staff Help still uses its own private renderer — unifying it onto KbContentService is a tracked follow-up. No schema change. |
| Jun 2026 | Public hub front door at `/` | The hub had no logged-out entry point — `/` was the internal staff dashboard. Added a `PublicLayout` + `Public/Landing` (standalone Inertia on the root design tokens, matching Login/ProposalView). `/` is now a public `LandingController` that branches by auth state: guests see the marketing landing; authenticated staff/super_admin redirect to the internal dashboard, referrers to `/referrer/dashboard`. The dashboard moved from `/` to `/dashboard` — its route NAME stays `internal.dashboard`, so every `route('internal.dashboard')` caller is unaffected (only the two literal `'/'` references — InternalLayout's Overview nav + a DashboardController fallback — were updated, and StaffLoginController's default destination switched from `'/'` to `route('internal.dashboard')`). No schema change. KB / Support / Become-a-partner render as inert "coming soon" until their phases land; the featured-help strip is deferred to Phase 2. Confirms public pages render via Inertia, not Blade. |
| Jun 2026 | Referral attribution Phase 1 (spine) | Extended the existing referral infra rather than rebuilding. `referrers` got a unique 8-char Crockford base32 `referral_code` (one universal `/r/{code}` link; backfilled + minted on create). New `referral_clicks` logs every valid-code hit; `AttributionService` resolves **last-touch within a 60-day window**, **new-customers-only**, **self-referral blocked**, writing ONE immutable row to **`customer_referrals` extended in place** (this is the spec's "referrals" table — not a new one; added lead_id/click_id/product/source/campaign/converted_at). Public lead path now captures the referrer (`leads.referrer_id`/`referral_code`) and `LeadController::convert()` calls the service (it previously dropped referral identity). Dead, FK-less `customers.referred_by` **dropped** (grep-confirmed no readers). New `AttributionSource`/`ProductKey` PHP enums. Internal `/referrals` ledger + referrer-portal link/click stats. Commission generation deferred to a later phase (no commission schema change). Cookie/?ref capture on external product sites + the `/api/referrals/attribute` ingest are Phase 3. |
| Jun 2026 | Additive `people` ↔ companies layer for multi-company ownership | "One person owning multiple companies" needed a cross-company human identity. Rather than refactor the ~55-file `contacts` surface, we added a NEW layer alongside it: a `people` table + a `customer_person` M:N pivot (role + job_title on the pivot, compound-unique on `(customer_id, person_id)`). `customers` stay one-company; `contacts` keep their NOT NULL `customer_id` one-to-many and their recipient-resolution role. A nullable `contacts.person_id` (set null, no v1 backfill) optionally links an operational contact to a person. `App\Enums\PersonRole` is the single source of truth for pivot roles (DB column is a plain string) — deliberately avoiding the value-list triplication that `contacts.role` suffers. `contacts` email-uniqueness relaxed from global to per-`customer_id` so one human can be a contact at several companies. No `is_primary` on the pivot in v1 (avoids overlap with `contacts.is_primary`). |
| Jun 2026 | Deal Registration — Sprint 1 (register → review → protect) | Partners register prospective deals from the portal; staff review; on approval a **fixed 90-day** protection window opens with the **clock starting at APPROVAL** (not registration). A registered deal is just a `Lead` with a new `referral_status` lifecycle (`pending_review`/`approved`/`rejected`/`expired`, PHP enum `App\Enums\ReferralStatus`) kept **separate from the pipeline `status` enum** — NULL means "not a deal-registration", so every existing manual/cookie-attributed lead is untouched. New nullable `leads` columns: `referral_status`, `registered_at`, `protected_until`, `reviewed_by`, `reviewed_at`, `review_notes`, `referral_consent_at` (indexed on `referral_status` + `protected_until`). All logic lives in `DealRegistrationService`: **register()** stamps `referrer_id` from the AUTHENTICATED referrer (never from input), sets `source=referral`, `status=new`, `referral_status=pending_review`, `registered_at`/`referral_consent_at=now`, and records the chosen product in `source_detail` (no product column added in Sprint 1); **approve()** sets `protected_until = now + config('referrals.protection_days', 90)`; **reject()** stores the reason in `review_notes`. **Dedup / net-new** is enforced at registration: match email → else phone → else company against existing customers/contacts AND leads; a match that is NOT the referrer's own prior registration throws a `ValidationException` with a deliberately **non-revealing** message (never leaks the existing record). Authorization via `LeadPolicy` (`register` = active referrer; `review` = staff) + `StoreReferralDealRequest` (required GDPR consent checkbox). Referrer portal gains a **Referrals** tab (own deals only, 20/page, IDOR-safe) + a "Register a deal" slide-over (product picker; self-serve product — `config('referrals.self_serve_slug')` — also shows the referrer's share link). Internal Leads gains a **Referral review** queue (Approve / Reject-with-reason); `pending_review`/`rejected`/`expired` deals are **filtered out of the sales kanban + KPIs** so sales only works approved/house leads, and approved deals show a "Protected — {referrer} until {date}" badge on the card + detail. Attribution spine unchanged except one guard: `AttributionService::attributeFromLead` now skips when `referral_status` is `rejected`/`expired` (approved + null attribute as before). |
| Jun 2026 | Deal Registration — Sprint 2 (protection expiry) | Added `referrals:expire-protections` (`App\Console\Commands\ExpireDealProtections`): selects leads where `referral_status = approved AND protected_until < now AND status != won AND customer_id IS NULL` and flips them to `expired` (one `DB::transaction` + `activity_log` `referral.deal_expired` per row; `protected_until` left as the historical date for audit). `chunkById(200)` keeps memory flat; **idempotent** — once a row is `expired` it no longer matches, so re-running is a clean no-op (covered by a test). Won/converted deals are explicitly excluded (the referrer earned them); pending_review / rejected / NULL leads are untouched. Registered in `routes/console.php` to run **daily at 03:30 Europe/London** (`withoutOverlapping()->runInBackground()`), 30 min after the `support:close-inactive` small-hours sweep — same pattern as the ~11 existing daily/interval schedules there (recurring + subscription invoices, reminders, suspensions, ticket auto-close, domain/website/WordPress syncs, overdue-notifications, health-cache warm), so a cPanel `schedule:run` cron is already presumed in place. `--dry-run` flag mirrors the other sweep commands. No schema/migration, no frontend. |
| Jun 2026 | Commission Engine — Sprint 1 (invoice-paid accrual) | Commissions now accrue automatically when an invoice is **paid** — the single settle point `StripeService::markInvoicePaid` (covers Stripe webhook `checkout.session.completed` + `payment_intent.succeeded` **and** the portal success-confirm, plus recurring subscription invoices). A new `CommissionService::accrueForInvoice` loads the paid invoice's lines (product_id lives on the **lines**, not the header), finds the customer's immutable `customer_referrals` attribution → an **active** referrer, groups lines by product, and writes one `pending` `commission_ledger` row per product via a **source-agnostic `accrue()` core** (so a future external/HMAC ingest reuses it unchanged). **5 decisions:** **(1)** The rule resolver + commission math were extracted from `MaavelusStatementService` into a shared `CommissionRuleResolver` + `CommissionService::calculate()`; Maavelus was refactored onto them with **zero behaviour change** (a feature test pins identical output). **(2)** The calculator gained **flat amount** (`config.flat_amount`, takes precedence over %) and a **per-rule recurring duration cap** (`config.recurring_months`; null/0 = lifetime); `recurring_tiered` stays stubbed at 0 (Sprint 2). **(3)** Maavelus stays on its own statement flow and is **excluded** from the invoice engine via `config('referrals.commission_excluded_slugs')` (default `['maavelus','maavelus-hospitality']`, mirroring `self_serve_slug`'s config-driven shape) so its revenue is never double-counted. **(4) Idempotency** is belt-and-braces: a new **unique index** `commission_ledger (invoice_id, referrer_id, product_id)` (invoice_id NULLable → MySQL NULLs distinct, so Maavelus null-invoice rows don't collide) **plus** an application-level already-accrued check — webhook retries / multi-path settles never double-credit. One-off accrual fires once per customer×product (`trigger=onboarding`); recurring fires per subscription-invoice (`trigger=invoice_paid`) until the cap. **(5)** The accrual is called at the **end of `markInvoicePaid`, after commit, only on the real unpaid→paid transition**, wrapped in `try/catch + report()` — a commission failure can never roll back or break the customer's payment. Approve/mark-paid lifecycle is unchanged (still manual via ReferrerController). Clawback (refund/churn) is Sprint 2. No frontend change — the portal already reads the ledger. |
| Jun 2026 | Commission Rules admin UI (internal) | A super_admin Settings surface (`/settings/commission-rules`, new SettingsLayout tab) to manage `commission_rules` — previously editable only by hand/seed. The form is a **faithful front-end to `CommissionService::calculate()`'s config contract**, exposing friendly inputs (not raw JSON): mode One-off vs Recurring, then percentage **or** flat amount (one-off → `type=one_off_pct`, `config{percentage}` or `{flat_amount}`), or a recurring percentage + duration Lifetime/N-months (recurring → `type=hybrid`, `config{recurring_percentage[, recurring_months]}`). `recurring_tiered` is **not exposed** (deferred/stubbed). `CommissionRuleService` translates inputs → type+config in a txn + activity_log; `StoreCommissionRuleRequest` validates (product required; at least one rate; percentages 0–100; flat ≥ 0; recurring_months positive int) + `CommissionRulePolicy` (super_admin). **OVERLAP GUARD:** the resolver picks one rule by ordering with no DB uniqueness, so the service **blocks creating/activating a second ACTIVE rule for the same (referrer_id, product_id) with an overlapping validity window** (inactive rules exempt) — resolution stays unambiguous. **EDIT SEMANTICS:** edits affect FUTURE accruals only (the engine computes at accrual time and ledger rows store their amounts); the form carries a note steering rate changes toward `valid_from`/`valid_until` versioning. Round-trip is tested (a rule created via the form produces config the calculator reads back to the expected amount). No schema change (`commission_rules` already existed); no migration. |
| Jun 2026 | Task due date made mandatory (`due_at`, validation-only) | A task with no schedule date is invisible in the MyWork buckets / kanban / calendar that all read **`due_at`** — the canonical scheduling field. Enforced `due_at` as **required, conditional on type**, in **validation only** (no DB NOT NULL): required for actionable types (task / call / meeting), **exempt for note + email** (logged activities that null `due_at` by design — requiring it would break the notes flow) and exempt when a timed event supplies `start_at` (the controller mirrors start_at→due_at, so `Rule::requiredIf` also skips when start_at is present, avoiding a double-reject on the calendar path). `TaskController` store + update share one `rules()`+`messages()`. **Divergence fixed:** `CustomerController::storeTask` previously wrote the **legacy `due_date`** column (so customer-created tasks never got a `due_at` and vanished from every scheduling view) — it now requires + writes `due_at`. **Programmatic creators get sane non-null defaults** so none are born invalid/overdue: `SupportController::createTask` → now+2d @09:00; `WorkflowEngine` actionCreateTask → now+(due_in_days ?? 3) @09:00; `TicketIntakeService` triage task → now + first_response_hours(priority) (the SLA deadline, never instant-overdue); `SyncTasksCommand` → milestone/project due date if present else now+7d @09:00. Frontend: every user-facing task form (Activities/Show edit + sub-task, Leads/Show, Customers/Show) marks the due field `required` for actionable types and surfaces the server error. **Tech debt:** the legacy **`tasks.due_date`** column is now **vestigial** — grep-confirmed no reader anywhere (only the `Task` model's fillable/cast list still names it; all other `due_date` references are invoice/project/milestone). It is a **candidate drop-migration** once the cast/fillable entry is also removed; left in place for now to keep this change schema-free. |
| Jun 2026 | Task due date — single-source rule + quick-add regression fix (Part C) | The Part A due-date policy had been copy-pasted across controllers, and one user-facing creator — `SupportController::createTask` — had slipped the net: it was treated as a programmatic creator (silent `now+2d` default) when it is actually a staff form. Part C extracts the policy into ONE place, **`App\Support\TaskDueDate`**: `rule()` (the conditional `Rule::requiredIf` — actionable task/call/meeting require `due_at`; note/email exempt; a timed event with `start_at` exempt), `messages()` (the single canonical message), and `resolve()` (note → null, bare YYYY-MM-DD → 09:00). It is reused by `TaskController` store/update, `SupportController::createTask`, and `CustomerController::storeTask`; the per-controller inline rules, the duplicate `parseDueAt`, and SupportController's silent `+2d` default are gone, while each controller keeps its own context (ticket_id, customer_id). **Regression fixed:** the Projects kanban quick-add (`Projects/Show.vue` `submitQuickAdd`) posted `type:'task'` with no `due_at`, so since Part A every quick-add 422'd with no error UI — it now has a required due-date input and surfaces `errors.due_at`. Remaining user surfaces brought in line: Support/Show (required for actionable, hidden + nulled for note, error shown), Dashboard (conditional client `:required`), MyWork (error line). A feature test asserts all three create endpoints reject a missing actionable `due_at` with the IDENTICAL message — proving the rule is defined once, not duplicated. No migration; the legacy `due_date` column remains the tracked drop-candidate. |
| Jun 2026 | Support Portal — Sprint 1 (SLA tracking + metrics) | First-response SLA tracking on support tickets, with **four locked decisions**: **(1) first-RESPONSE is the hard SLA** — `sla_breach_at` (already = created_at + per-priority hours) is reinterpreted as the first-response deadline; `first_responded_at` is stamped once, on the FIRST staff non-internal reply (`SupportSlaService::stampFirstResponse`, idempotent — a second reply or an internal note never moves it). **(2) Resolution time is MEASURED, not committed** — analytics shows avg RAW resolution (`resolved_at − created_at`, no pause), with no resolution target. **(3) Per-priority targets are CONFIG-DRIVEN** — the SLA_HOURS map moved out of TicketIntakeService into `config/support.php` (`first_response_hours`: urgent 4 / high 8 / medium 24 / low 72), read via `SupportTicket::firstResponseHours()` (single source; can later key on product/tier). **(4) Breach is COMPUTED ON READ** — `SupportTicket::slaState()` → `App\Enums\SlaState` (met = responded ≤ deadline; due = unresponded & in-window, with `slaRemainingSeconds()` countdown; breached = responded late OR unresponded past deadline). No stored breach flag, no sweep. New columns: `first_responded_at`, `reopened_at`, `reopen_count`. Reopen (resolved|closed → open) is centralised in `SupportSlaService::recordReopen`, hooked through the three paths (staff reply, status change, inbound-email reply) so they stay consistent — stamps `reopened_at` + increments `reopen_count` only on a genuine closed→open transition (awaiting_customer → open is not a reopen). Helpdesk index + show render an SLA badge (countdown / MET / BREACHED) from the derived state. AnalyticsController gains a support section (avg first-response, % within SLA, breach count/rate, avg raw resolution, reopen rate, volume by status/priority/product) + a panel in the analytics page. CSAT joins this panel in Sprint 2. |
| Jun 2026 | Forms theming — Phase 2b (design editor UI) | A staff-facing design editor for the reusable `form_themes` (created in 2a). `Internal\FormThemeController` (CRUD, 20/page) + `StoreFormThemeRequest` (validates name + every token) + `FormThemePolicy` (manage = `isStaff`; **`manageCustomCss` = super_admin only**) + `FormThemeService` (txn + activity_log; normalises tokens to the canonical key set, empty→null, drops unknowns). Routes live under the existing Forms/Operations area (`/forms/themes`, nested in the `internal.forms.` group, defined before the whereNumber `/{id}` routes so "themes" never collides). New `Forms/Themes/Index.vue` design editor: swatch+text colour fields for every colour token (text field authoritative so `transparent`/`rgba()` work), font family/size, radius, border width, button style (solid/outline), full-width, logo URL, heading. **`custom_css` is super-admin-only end-to-end** — read-gated (stripped from every Inertia payload incl. `default_tokens`), write-gated (ignored from non-super-admins; a staff edit preserves an existing super-admin value rather than wiping it), and the editor section is hidden unless `can.manage_custom_css`. The form builder gains a **theme selector** (`forms.theme_id`, "Default" = null) wired through `FormBuilderController` validate/store/update/map; the builder copy notes that editing a theme restyles every form using it and embeds refresh within ~5 min (embed.js `max-age=300`). **The faithful live preview was deliberately split to Phase 2c** — rendering the REAL widget needs a CSP-aware internal render route (the app CSP is `frame-ancestors 'none'`) + a debounced iframe; the misleading Vue form-preview is untouched for now and will be replaced in 2c. No migration (form_themes + forms.theme_id already exist from 2a). |
| Jun 2026 | Form fields — per-field width (multi-column layout) | Forms can now be multi-column: each field carries a `width` (full / half / third) via a new `form_fields.width` string column (DEFAULT `'full'`) + `App\Enums\FieldWidth` (cast on the model; `cols()` → 12/6/4 on a 12-col grid). **Zero visual change for existing forms** — every field defaults to full, i.e. today's single-column stack (pinned by a test). The embed widget wraps fields in a `.pw-grid` (12-col CSS grid, 14px gap replacing the old per-row margin so single-column rhythm is unchanged); each field's `.pw-row` gets `pw-col-12|6|4`. Submit button, GDPR row, and logo/heading stay outside the grid → always full; the honeypot stays hidden + outside the grid. **Responsive by CONTAINER width, not viewport**: `container-type:inline-size` on the grid + an `@container (max-width:480px)` rule collapses half/third to full on narrow embeds regardless of the host page's viewport; a `ResizeObserver` fallback toggles a `.pw-grid--narrow` class only where `@container` is unsupported. The builder field editor gains a Width control (Full/Half/Third), threaded through `FormBuilderController` validate (`Rule::enum(FieldWidth::class)`, null→full) / store / update (the existing wipe-and-recreate field strategy) / card map. All submit/spam/validation hooks (field name, _hp, _gdpr, pw-err-*, FormData/fetch, success/redirect) preserved. One migration (`form_fields.width`); SCHEMA updated. |
| Jun 2026 | Form themes — form-container styling tokens | Extended the theme token set with four form-CONTAINER controls: `form_padding`, `form_border_width`, `form_border_radius`, `form_border_color` (mirroring the field-level border controls but applied to the `.pw-form` element itself). **No migration** — tokens live in `form_themes.tokens` JSON and `FormThemeTokens::resolve()` is generic, so it back-fills the new defaults for every existing theme automatically. **Defaults are no-effect** (`form_padding 0`, `form_border_width 0`, `form_border_radius 0`, `form_border_color #d1d5db` — moot at width 0), so existing themes and forms are **pixel-identical** (pinned by tests: the defaults set + an existing theme with none of the new keys renders the no-effect values). The widget's `.pw-form` rule now consumes `--pw-form-padding` / `--pw-form-border-width` / `--pw-form-border-radius` / `--pw-form-border-color` (+ `box-sizing:border-box` so `max-width:560px` includes any padding — harmless at 0). The design editor gains a "Form container" section (padding / border width / border radius text inputs + a swatch+text border-colour field). `StoreFormThemeRequest` validates the four keys; the field grid + all submit/spam/validation hooks are untouched. SCHEMA token-key list updated. |
| Jun 2026 | Form themes — modern button hover effects | Replaced the plain colour-darken button hover with a theme-configurable `button_hover` token: `none` / `darken` (today's bg-darken) / `lift` / `glow`, **default `lift`** (an intentional modernisation — existing themes back-fill to lift, so their buttons now lift instead of darken). No migration (JSON token; `FormThemeTokens::resolve()` back-fills). The widget gives ALL buttons a baseline `transition:transform/box-shadow/background/color .15s ease`, a `:active` press (translateY(1px)), and a `:focus-visible` accent outline; the mode is applied via a `pw-btn-hover-{value}` class on the button. Effects: **lift** = `translateY(-1px)` + soft neutral shadow on hover, press returns to 0; **glow** = a translucent coloured ring via `color-mix(in srgb, var(--pw-button-bg) 25%, transparent)` (graceful no-op where color-mix is unsupported); **darken** = the old bg-darken (and the outline fill stays scoped to darken mode); **none** = no change. Works for both solid + outline. Design editor gains a "Button hover" dropdown; `StoreFormThemeRequest` validates `in:none,darken,lift,glow`. All submit/spam/validation hooks untouched. SCHEMA token list updated. |
| Jun 2026 | Form themes — modern button treatments + icons (supersedes basic hover) | Replaced the earlier hover set with a modern one and added optional submit-button icons, all theme-configurable (no migration; JSON tokens back-filled). `button_hover`: `none` / `lift` (translateY(-2px) + soft elevation) / `glow` (translucent coloured ring + halo via `color-mix` from the button colour) / `shine` (light sheen sweep via a clipped `::after` gradient) / `fill` (colour overlay wiping in from the left via a `::before` behind the text) — default `lift`; **`darken` was dropped** (a stored `darken` simply yields no hover effect — graceful). `button_icon`: `none`/`arrow`/`send`/`chevron`/`check`/`download` (default none); `button_icon_position`: `leading`/`trailing` (default trailing). Widget bakes a small inline SVG set (stroke=currentColor, sized 1em so it inherits colour + scales with the font); directional icons (arrow, chevron) slide on hover. All buttons get tasteful easing (`cubic-bezier(.4,0,.2,1)`, ~200ms) across transform/shadow/background/colour, an `:active` press, and a `:focus-visible` outline; effects work for solid AND outline. The submit label is now its own span so the "Sending…" text swap no longer wipes the icon. Design editor gains Button hover / Button icon / Icon position dropdowns; `StoreFormThemeRequest` validates all three enums. All submit/spam/validation hooks preserved. SCHEMA token list updated. |
| Jun 2026 | Domains priced via product-plans + expiry-triggered renewal billing (Slice 1) | Domains now carry a **pricing plan** but bill off their **expiry date**, not the subscription cron. New `product_plans.is_domain` (sibling to `is_hosting`) marks domain-registration plans; a "Domains" product + domain plan(s) carry the renewal price via the existing `product_plan_prices` + the product's `billing_entity_id`. New `domains.product_plan_id` (nullable FK → product_plans, nullOnDelete) is the **price source** — domains stay first-class in their own table (registrar/expiry/DNS/auto_renew) and are **NOT CustomerProducts**, so `invoices:generate-subscriptions` never touches them (pinned by a test). New daily command **`invoices:generate-domain-renewals`** (08:00 Europe/London, withoutOverlapping, after the 07:30 subscription sweep): selects domains where `auto_renew = true AND product_plan_id IS NOT NULL AND expiry_date ∈ [today, today+14d] AND renewal_invoiced_for ≠ expiry_date`, then builds a draft `Invoice` + `InvoiceLine` inline (mirroring `GenerateSubscriptionInvoices`: 20% VAT, +14d terms, `type=subscription`, plan default price, billing entity from the plan's product, `Invoice::generateNextNumber()` lock, `DB::transaction` + system `activity_log`). **Idempotency** via `domains.renewal_invoiced_for` (the billed expiry cycle): re-runs in the same cycle are no-ops; the next cycle becomes billable when a registrar sync advances `expiry_date`. Domain management UI (`DomainController` + `/domains` slide-over) gains a renewal-plan selector limited to active `is_domain` plans (`Rule::exists(...)->where('is_domain', true)`). Two migrations; SCHEMA updated. |
| Jun 2026 | Domains renewal — TLD-driven model + per-plan renewal term (refines Slice 1) | Replaced the manual "Renewal plan" selector with a **TLD → plan match**. A domain's `tld` (new `domains.tld`) matches an active `is_domain` product plan (new `product_plans.tld`, required for domain plans; **one active plan per TLD** enforced in `ProductPlanController` so the match is unambiguous) — that plan is the price source. The plan also carries a **renewal term** (`product_plans.renewal_interval_count`/`renewal_interval_unit`, default 1yr) selecting WHICH price tier the auto-renewal bills (e.g. 2 years for `.gr`'s 2-year minimum) — it sets the price/amount only; the trigger stays 14 days before expiry and idempotency stays per expiry cycle (`renewal_invoiced_for`). Plan form (`Settings/ProductPlans.vue`): "Domain plan" toggle → TLD field + renewal-term picker. Domain form (`Domains/Index.vue`): the plan dropdown became a **TLD dropdown** (from active domain-plan TLDs) with the TLD **pre-suggested** from the domain name (longest-suffix match, handles `.co.uk` vs `.uk`); auto_renew unchanged. `domains.product_plan_id` is kept as a DERIVED cached link (set on save + by the command from the TLD). `GenerateDomainRenewalInvoices` now resolves the plan by TLD (keyed map of active domain plans) and bills the renewal-term tier price (falls back to default/first active); TLD with no active plan → skip. Everything else as Slice 1 (14-day window, auto_renew, VAT, billing entity from the plan's product, created_by, DB::transaction + activity_log). Two migrations; SCHEMA updated. |
| Jun 2026 | Invoicing fixes folded into the domains work | (1) **`GenerateSubscriptionInvoices` was latently broken** — it never set `created_by`, and `invoices.created_by` is NOT NULL with no default, so every due `auto_invoice` subscription would throw on insert and be silently skipped (0 ever billed via its path). Fixed by resolving a system user once before the loop (super_admin, else first user, null-guarded) and setting `created_by` on the invoice — same pattern as `GenerateDomainRenewalInvoices`. Regression test added (a due active auto_invoice subscription now bills with `created_by` set). (2) **Renewal-term price-tier guard** — `ProductPlanController` now rejects a domain plan whose renewal term has no matching active price tier (e.g. a 2-year term with only a 1-year tier; candidates = existing active prices ∪ the initial tier; deferred only when the plan has no tiers yet), surfaced on the plan form. `GenerateDomainRenewalInvoices::resolveRenewalPrice` now returns null on a term/tier mismatch and the command **skips + reports** that domain rather than billing a fallback price. Tests cover both (form rejects mismatch; command skips at runtime). No schema change for these fixes. |
| Jun 2026 | Deployment maintenance under Settings (replaces public helper scripts) | The deploy bundles shipped public `migrate.php` / `clear-cache.php` helper scripts — unauthenticated PHP that runs migrations/clears caches if left web-reachable. Replaced with **Settings → Deployment** (`Internal\DeploymentController`, `/settings/deployment`), super_admin-gated by the existing `role:super_admin` settings route group **plus** a `manage-deployment` Gate ability (defined in AppServiceProvider) authorised in every action. Preview shows pending migrations (names + Ran/Pending, via the `migrator` — the same data `migrate:status` shows — with the raw `migrate:status` text too), guarding the fresh-DB no-table case. Actions (each ConfirmModal-confirmed, `throttle:10,1`, output captured + flashed back, logged to `activity_log` as `deployment.*` with who/when/ok/output-summary): **Run migrations** (`migrate --force`), **Clear caches** (`optimize:clear`), **Run both**; best-effort guarded `opcache_reset()` after migrating. The controller is deliberately **self-contained** — it touches only Artisan + the long-stable `activity_log` table and loads no app models/relations that could reference a not-yet-migrated column, so the page stays reachable immediately post-deploy (before `migrate` runs). New SettingsLayout "Deployment" tab. No migration for the feature itself. Going forward, deploy = extract bundle → open Settings → Deployment → Run both (the `clear-cache.php` in bundles remains as a CLI fallback). |
| Jun 2026 | Domains renewal — simplified to one-tier (dropped the renewal-term field) | Domains bill ONLY via auto-renewal (no register-for-N-years flow), so a domain plan needs exactly ONE price tier — its interval IS the renewal duration, its price IS the renewal price. The separate `renewal_interval_count`/`renewal_interval_unit` plan columns + the renewal-term form field + the term/tier-match logic were redundant and removed. **Migration handling:** the rework migration `2026_06_07_120000` (never deployed) was AMENDED in place to add only `tld` (not the renewal-term columns) rather than leaving an add-then-drop pair; the local dev DB's orphaned columns were dropped to match. **Invariant:** an active `is_domain` plan must have exactly one active price tier — enforced in `ProductPlanController` (store/update) AND `ProductPlanPriceController` (blocks adding/activating a second active tier on a domain plan), gated to active plans (inactive domain plans are exempt). `GenerateDomainRenewalInvoices` now bills the plan's single active tier (duration via the tier's interval label, price = tier price); the renewal-term resolution + term/tier-mismatch skip were removed, replaced by a simple no-active-tier skip+report. Tests updated: a domain plan requires exactly one active tier; the command bills that tier for its duration (covers a single 2-year tier @ £20 billing £20). No new migration; SCHEMA updated. |
| Jun 2026 | Websites — customer-consistency guard on cross-links | A website's `domain_id` / `customer_product_id` / `project_id` are validated `exists:<table>,id` only — existence, not ownership — so a forged/stale id could link a site to another customer's domain, hosting plan, or project (a cross-tenant integrity leak; the `nullOnDelete` FKs don't constrain ownership). Added `WebsiteController::guardLinksBelongToCustomer($data)`, mirroring `TaskController::guardContactBelongsToCustomer` in shape + lifecycle (private method, run AFTER `validateRow` and BEFORE the `DB::transaction`, in BOTH store and update). Each provided link must have `customer_id` == the website's `customer_id` (`$data['customer_id']` — required by validateRow and, on update, the value being written). Deviation from the contact guard: it raises a **per-field** `ValidationException::withMessages` (one message per offending select: "Selected domain / hosting plan / project does not belong to the chosen customer.") rather than a single `abort_unless(422)`, so each dropdown surfaces its own error. Null/absent links stay allowed; `projects.customer_id` is nullable, so a customer-less project linked to a customer site correctly diverges and is rejected. No schema/validation-rule change (guard is post-validation). Feature test covers store + update: rejects a different-customer domain/product/project, accepts same-customer links, accepts null links. |
| Jun 2026 | Billing redesign Stage 1a — hosting becomes a property of the website; domain-as-subscription removed | First step off "CustomerProduct as the universal billing wrapper" (billing-ADJACENT; no new invoicing — that's 1b). **Hosting moved onto the website:** new `websites` columns `plan_id`/`plan_price_id` (FK product_plans / product_plan_prices, nullOnDelete), `hosting_status` enum(none/active/suspended), `hosting_started_at`, plus billing anchors `hosting_auto_invoice`/`hosting_next_billing_date`/`hosting_last_invoiced_at` that **mirror CustomerProduct's billing fields and are inert until Stage 1b**. The website add/edit slide-over now picks an `is_hosting` plan + active price tier straight from the **catalog** (new top-level `hosting_plans` prop = active is_hosting plans + active tiers) — the old `hosting_products` active-CP dropdown source is gone, so **no "enable product" pre-step / no plan pre-activation** is required (`WebsiteController::applyHostingState` sets status=active + started_at on attach, none on clear; validates the tier is an active price of the chosen plan; `plan_id` is rule-gated to `is_hosting`). The website card now shows the website's OWN plan (`plan`/`planPrice`) instead of the linked CP's `plan_name`. `websites.customer_product_id` + the WebhookDispatcher cascade are left **untouched/inert** (0 links on prod; re-route is Stage 1b). **Domain-as-subscription removed:** new `App\Support\DomainCustomerProductConverter` (run by a data migration) converts every `is_domain` CustomerProduct into a self-billing `Domain` — it ensures a Domain exists first (find by customer+TLD, else create from the CP), back-fills `tld`/`product_plan_id`/`auto_renew` so it can self-bill, then deletes the CP; it **never deletes a domain CP without a Domain**. Data-driven + idempotent (no-op where there are no domain CPs); on prod it converts the single mislabeled id=1 ("WhiteDash .co.uk"), which already has its Domain, so it just back-fills + deletes the CP. **Recurrence blocked:** `is_domain` plans are excluded from the enable picker in BOTH `CustomerController` and `ProvisioningController`, and rejected server-side in `enableProduct` + `ProvisioningController::toggle`. Two migrations (websites hosting columns; data conversion); SCHEMA updated. Tests: a website attaches a catalog hosting plan with no pre-enabled CP; attach sets status/started; clear sets none; the converter creates-then-deletes and back-fills-then-deletes; the picker + guard exclude is_domain. |
| Jun 2026 | Billing redesign Stage 1b — per-website hosting billing + hosting suspend/reinstate lifecycle | The money path for hosting now that it lives on the website (Stage 1a). **No schema** (reuses the Stage-1a `websites.hosting_*` columns). **New command `invoices:generate-hosting`** (scheduled 07:45, between the 07:30 subscription + 08:00 domain sweeps) mirrors `GenerateSubscriptionInvoices`' cadence but sources from `Website`: selects `hosting_status='active' AND hosting_auto_invoice=1 AND plan_id & plan_price_id set AND hosting_next_billing_date <= today`; per website in a `DB::transaction` it builds a draft `Invoice` + `InvoiceLine` from the catalog `plan_price` (type `subscription`, VAT 20%, 14-day terms, `created_by` = super_admin ?? first user, billing entity = the plan's `product.billing_entity_id ?? first active` — same resolution as the domain command), then **advances `hosting_next_billing_date` by the tier interval (`interval_count`/`interval_unit`, addMonths/YearsNoOverflow) and stamps `hosting_last_invoiced_at` IN THE SAME txn** — so a same-day re-run re-selects nothing (idempotent, pinned by a test). A defensive in-loop guard also skips any non-active website; `dash.mrr` is busted; every invoice is `activity_log`ged. **Billing controls** added to the website edit form — a "Bill hosting automatically" toggle (`hosting_auto_invoice`) + a "Next billing date" anchor (`hosting_next_billing_date`), validated in `WebsiteController` and only meaningful when a plan is attached (`applyHostingState` clears them when the plan is cleared). **Suspend/reinstate lifecycle**: `WebsiteController::suspendHosting`/`reinstateHosting` (POST routes, `authorize('update', $website->customer)`) flip `hosting_status` active↔suspended; suspended hosting is excluded from the sweep by the billing filter (so suspend stops billing, reinstate resumes it — pinned). **WebhookDispatcher cascade re-route**: added `dispatchHostingSuspension`/`dispatchHostingReinstatement(Website)` that fire the suspend/reinstate webhook keyed on the WEBSITE (to the hosting plan's product slug — a graceful no-op when that product has no configured endpoint, which it doesn't today) and run the WHM cPanel cascade for THAT website; the existing CustomerProduct path (`dispatchSuspension`/`dispatchReinstatement`, used by CustomerProductController / ProcessSuspensions / StripeService for genuine SaaS) is left entirely intact. No migration; no cache-clear required beyond the standard asset rebuild. Tests: bills active+auto+due (price/entity/VAT/line); advances monthly AND annual + stamps last-invoiced; does NOT bill not-due/not-auto/suspended/no-plan; idempotent (twice → once); suspend excludes / reinstate re-includes; suspend/reinstate fire the website webhook. |
